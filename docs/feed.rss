<?xml version="1.0" encoding="UTF-8" ?><rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Justin Purnell</title><description>Justin Purnell — Founder of Ledge Partners. Former Goldman Sachs credit analyst, Head of Product at Hotels at Home, VP at NBCUniversal. Princeton &apos;00, Tuck MBA.</description><link>https://www.justinpurnell.com</link><atom:link
    href="https://www.justinpurnell.com/feed.rss"
    rel="self" type="application/rss+xml"
/><language>en</language><generator>Ignite v0.6.0</generator><image><url>https://www.justinpurnell.com/images/logos/rss.png</url><title>Justin Purnell</title><link>https://www.justinpurnell.com</link><width>144</width><height>152</height></image><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/gate/documentation-that-cannot-lie</guid><title>&quot;Documentation That Cannot Lie&quot;</title><link>https://www.justinpurnell.com/QualityGate/gate/documentation-that-cannot-lie</link><description><![CDATA[Every linter checks whether your documentation exists. Almost none check whether it’s true.]]></description><pubDate>Thu, 13 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[gate]]></category><category><![CDATA[documentation]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Every linter checks whether your documentation exists. Almost none check whether it’s true.</strong></p><hr /><p>Ask a static analysis tool about your documentation and it will tell you three things: whether the public API has doc comments, whether the markup parses, and whether the links resolve. All useful. All silent on the only question a reader actually has.</p><p><em>Does it work?</em></p><p>The code sample in your README, the one someone will paste into a playground in about four minutes — does it compile? Does it run? The output it claims to produce, does the program actually produce that? The table of modules in your architecture document, the one that says thirty-four when the package has sixty-three — what would have caught it?</p><p>Nothing. Documentation is the one artifact in a modern codebase that nobody compiles, and that exemption is why it rots faster than anything else you ship.</p><h2>The ladder</h2><p>There’s a natural progression here, and each rung is a strictly stronger claim than the one below it.</p><p><strong>Rung 0 — it exists.</strong> Every doc-coverage tool. Necessary, and the easiest to satisfy without saying anything: a comment reading <code>/// The name.</code> above <code>var name: String</code> passes.</p><p><strong>Rung 1 — it compiles.</strong> Take every fenced Swift block in an article, concatenate them in document order, and typecheck the result against the built module. This is a bigger step than it sounds, because it forces a decision: is an article <em>one program</em> or a collection of fragments? One program is the right answer — it’s what a reader does when they paste — and it means a later block referring to an earlier binding is <em>correct</em>, while two independent examples that both open with <code>let data = …</code> are a defect in the article whose repair is a rename.</p><p>Pointed at one 73-article catalog, this found <strong>2,092 errors across 67 of them</strong> — including three constructors documented with argument labels the API never had, and a type documented as public that was internal.</p><p><strong>Rung 2 — it runs.</strong> Compiling proves the types line up. It doesn’t prove the example doesn’t trap on the second line. Running the article top to bottom catches the sample that typechecks beautifully and force-unwraps a nil.</p><p><strong>Rung 3 — its numbers are real.</strong> An article that says <em>“this returns 0.847”</em> is making a claim that can be checked: run the program, capture what it prints, compare. This is the rung where documentation stops being prose that resembles the truth and starts being a test.</p><p>There’s a trap here worth naming, because it’s the reason this rung is opt-in and always will be: the obvious “fix” for a failing number is to rewrite the documentation to match the program. That’s not a fix, it’s laundering. If the number changed because of a regression, updating the doc erases the only artifact that objected. Rung 3 has to fail loudly and be repaired by <em>deciding which value is right</em> — a judgment no tool should make.</p><h2>The fourth thing, which isn’t a rung</h2><p>The ladder handles code in prose. It says nothing about prose <em>derived</em> from code — the module table, the checker reference, the error registry, the changelog’s link definitions. That content isn’t written so much as transcribed, and transcription decays.</p><p>The fix is to stop transcribing. Wrap the derived content in delimiters, name the thing that generates it, and let the gate regenerate it in memory and compare:</p><pre><code class="language-markdown"><!-- generated:module-structure -->
- `QualityGateCore` — Shared protocol, models, reporters
…
<!-- /generated:module-structure -->
</code></pre><p>Everything between the delimiters belongs to the generator. Everything outside belongs to the author. Drift becomes a compile-time-shaped failure: not “this document feels stale” but “line 47 says something the tree does not.”</p><p>Turned on for the first time against the project that built it, this reported <strong>65 findings</strong>: twenty-nine modules absent from the architecture table, seventeen from the status roster, an error registry that had never listed a case which shipped with an entire feature, and four changelog link definitions that had never been written <em>at all</em> — every version heading rendering on GitHub as literal text with brackets around it, for four releases.</p><h2>Why this is harder than it looks</h2><p>Three problems eat most of the implementation, and they’re the interesting part.</p><p><strong>A generator has to be trustworthy enough to overrule a human.</strong> If the tool regenerates a table and deletes a row, it had better be right. One naive version would have deleted the row documenting a feature that had <em>moved</em> — turning a stale row into a missing one and calling that green. Generation doesn’t eliminate editorial judgment; it relocates it somewhere less visible.</p><p><strong>Membership and state are different questions.</strong> A checklist of modules with tick-boxes has two columns and only one is derivable. <em>Which modules deserve a line</em> is a set, exactly. <em>Whether the work is done</em> is a judgment no tool can make, and deriving it from “a directory exists” would produce sixty-three green boxes meaning only that sixty-three folders are present. So the generator owns membership and never touches a tick-box — it adds and removes lines and rewrites none.</p><p><strong>Some documents look derivable and aren’t.</strong> A roadmap is the obvious next candidate: it’s a checklist, it names artifacts, it goes stale. Measured, a generator deriving its membership from the package would have deleted <strong>19 of 38 items</strong> — every entry describing work not yet done, which is what a roadmap <em>is</em>. A roadmap is a roster of intentions, and an intention isn’t derivable from a tree, because the tree is what exists and the roadmap is the list of what doesn’t. That one got measured before it got built, and the measurement killed it.</p><h2>The property underneath all of it</h2><p>Every failure above shares a shape, and it’s worth stating on its own:</p><blockquote><p><strong>A checker that examined nothing must not print what a checker that found nothing prints.</strong></p></blockquote><p>The generated-content checker reported its coverage from day one — regions found, regions regenerated, generators unused — on every run, pass or fail, including when the answer was zero. It has never had this class of failure.</p><p>Its siblings, written without that discipline, both did. One examined a single module out of 116 and printed a pass. Another would have discovered zero articles in a package laid out with <code>Source/</code> instead of <code>Sources/</code> and printed the same pass. Neither was lying about what it found. Both were silent about what they’d looked at, and silence reads as success.</p><p>If you build one thing from this post, build that. Make every checker state its coverage, and make zero coverage an error rather than a pass. It costs a line of output and it closes a category of bug you would otherwise meet one instance at a time, for years.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check doc-code           # rung 1: the examples compile
quality-gate --check doc-comment-code   # rung 1, applied to /// comments
quality-gate --check doc-run            # rung 2: they run
quality-gate --check doc-claims         # rung 3: their numbers are real
quality-gate --check doc-generated      # derived prose matches the tree
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/specialty/consistency</guid><title>&quot;ConsistencyChecker: Where the Gate Becomes the Mirror&quot;</title><link>https://www.justinpurnell.com/QualityGate/specialty/consistency</link><description><![CDATA[The checker that ends every gate run with a question the code alone can’t answer: is this decision consistent with everything the organization has learned?]]></description><pubDate>Fri, 31 Jul 2026 09:49:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[specialty]]></category><category><![CDATA[mirror]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>The checker that ends every gate run with a question the code alone can’t answer: is this decision consistent with everything the organization has learned?</strong></p><hr /><p>Every other checker in the suite asks about the code in front of it. <code>ConsistencyChecker</code> asks about the code in front of it <em>in the context of every decision that came before</em> — and that makes it the seam where the <a href="https://www.justinpurnell.com/QualityGate/mirror/the-mirror">Gate becomes the Mirror</a>.</p><h2>What it does</h2><p>At the end of a gate run, it scores the current results against the <a href="https://www.justinpurnell.com/QualityGate/mirror/the-mirror">Institutional Judgment System’s</a> latest <strong>pulse</strong> — the statistical summary of the organization’s recent override and failure patterns. It asks three questions:</p><ul><li><strong>Cluster match</strong> — does this failure match a known violation cluster? If the same rule has been failing and getting overridden for weeks across projects, that’s institutional drift, not a one-off.</li><li><strong>Anomaly pattern</strong> — does this checker’s failure rate deviate from the statistical baseline? A sudden spike might indicate a systemic issue.</li><li><strong>Unaddressed policy</strong> — was a policy change proposed for this pattern and never implemented? That’s institutional debt.</li></ul><p>Each match is a <code>ConsistencyFinding</code> with a risk weight, and the score rolls up from 1.0 (fully consistent with institutional history) downward — with <strong>validity-aware discounting</strong>, so a finding based on 3 data points carries a quarter the weight of one based on 30+. A score of 0.85 means “mostly consistent.” A 0.4 means “this looks like drift — the pulse shows patterns you should know about.”</p><h2>The bridge</h2><p>So a gate run no longer just answers “does this pass?” It answers “does this pass, <em>and</em> is the decision to ship it consistent with what we’ve learned?” The first question is mechanical. The second is judgment — and <code>ConsistencyChecker</code> is where the mechanical gate reaches into the institutional memory and hands the judgment back to you, quantified.</p><p>That’s the whole thesis of the project in one checker: enforcement and memory, the gate and the mirror, in the same run. The rest of the story is in the <a href="https://www.justinpurnell.com/QualityGate/mirror/the-mirror">Mirror</a> posts.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check consistency
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/specialty/disk-clean</guid><title>&quot;DiskCleaner: The Opt-In Checker That Once Faked a 61-Error Run&quot;</title><link>https://www.justinpurnell.com/QualityGate/specialty/disk-clean</link><description><![CDATA[Build-artifact and cache cleanup, as a checker — and a cautionary tale about what happens when a destructive tool runs at the wrong time.]]></description><pubDate>Fri, 31 Jul 2026 09:48:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[specialty]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Build-artifact and cache cleanup, as a checker — and a cautionary tale about what happens when a destructive tool runs at the wrong time.</strong></p><hr /><p><code>DiskCleaner</code> reclaims disk from build artifacts and caches — the <code>.build</code> directories, DerivedData, index caches that pile up across a portfolio of projects. It’s <strong>opt-in</strong>, excluded from default runs, and the reason it’s opt-in is a lesson worth telling.</p><h2>The 61-error run that wasn’t</h2><p>Once, while dogfooding the full suite with <code>--check all</code>, the gate reported 61 errors. Alarming — until I traced it. <code>disk-clean</code> had been included in the run, and it did exactly what it’s supposed to: it wiped <code>.build</code>. The <em>next</em> checker to run was the build checker, which now faced an empty build directory and reported the whole project as failing to compile. Sixty-one errors, all an artifact of a destructive cleanup running mid-gate, ahead of the checkers that needed the build tree it just deleted.</p><p>Nothing was actually wrong with the code. The “failure” was a sequencing accident: a tool that removes build state has no business running in the same pass as tools that depend on it.</p><h2>The lesson, encoded</h2><p>That incident is why <code>disk-clean</code> is opt-in and why the guidance is explicit: <strong>exclude <code>disk-clean</code> when dogfooding a full run.</strong> It’s a genuinely useful tool for reclaiming space across many checkouts — just not something you want firing automatically alongside the checkers that assume a built tree exists. The checker still ships; it just refuses to be a default, because a destructive default is a footgun waiting for a hurried <code>--check all</code>.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check disk-clean          # deliberate, opt-in cleanup
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/specialty/xcode-build</guid><title>&quot;XcodeBuildChecker: For the Projects SwiftPM Can&apos;t See&quot;</title><link>https://www.justinpurnell.com/QualityGate/specialty/xcode-build</link><description><![CDATA[Not every Swift project is a clean SwiftPM package. This checker drives xcodebuild for the ones that aren’t — and generates the IndexStore the deeper auditors need.]]></description><pubDate>Fri, 31 Jul 2026 09:47:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[specialty]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Not every Swift project is a clean SwiftPM package. This checker drives <code>xcodebuild</code> for the ones that aren’t — and generates the IndexStore the deeper auditors need.</strong></p><hr /><p><code>XcodeBuildChecker</code> is <strong>opt-in</strong> — excluded from default runs unless you explicitly ask for it — because it’s for a specific situation: a project that’s an <code>.xcodeproj</code>/<code>.xcworkspace</code> rather than a plain SwiftPM package, or one that needs a real Xcode build to produce a usable IndexStore.</p><h2>Two jobs</h2><ul><li><strong>Build validation.</strong> It drives <code>xcodebuild</code> and captures the result as gate diagnostics, so an Xcode-project build is gated the same way <code>swift build</code> is for a package.</li><li><strong>IndexStore generation.</strong> Several of the most powerful checkers — <a href="https://www.justinpurnell.com/QualityGate/correctness/unreachable">unreachable</a> cross-module analysis, <a href="https://www.justinpurnell.com/QualityGate/correctness/complexity">complexity</a> amplification — depend on an up-to-date IndexStore to cross-reference symbols across the whole project. For projects where SwiftPM doesn’t produce one usable directly, this checker builds it.</li></ul><p>That’s why it’s paired with the <code>--auto-build-xcode</code> flag: when an index-backed checker needs a fresh IndexStore and finds a <a href="https://www.justinpurnell.com/QualityGate/correctness/unreachable">stale one</a>, the gate can drive <code>xcodebuild</code> to regenerate it rather than running against data it can’t trust.</p><h2>Why opt-in</h2><p>An <code>xcodebuild</code> invocation is slow and environment-specific — it shouldn’t run on every fast local gate. Keeping it opt-in (like <a href="https://www.justinpurnell.com/QualityGate/specialty/disk-clean">disk-clean</a>) means the default gate stays fast, and the heavier Xcode path runs only when a project actually needs it. Right tool, right cadence, explicit choice.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check xcode-build
quality-gate --check unreachable --auto-build-xcode    # regenerate the index if stale
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/specialty/appintents-readiness</guid><title>&quot;AppIntentsAuditor: The Siri Integration That Fails at Registration&quot;</title><link>https://www.justinpurnell.com/QualityGate/specialty/appintents-readiness</link><description><![CDATA[App Intents power Siri, Shortcuts, and Spotlight — and they fail in ways the compiler often accepts but the system rejects at registration.]]></description><pubDate>Fri, 31 Jul 2026 09:46:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[specialty]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>App Intents power Siri, Shortcuts, and Spotlight — and they fail in ways the compiler often accepts but the system rejects at registration.</strong></p><hr /><p>Apple’s App Intents framework is protocol-heavy: an entity must conform just so, parameters need the right property wrappers, metadata protocols must be present. Get it subtly wrong and the code frequently <em>compiles</em> — then the intent doesn’t register, or registers without the behavior you expected, and you find out from a Shortcut that silently does nothing. <code>AppIntentsAuditor</code> checks the conformances at commit time.</p><h2>What it checks</h2><ul><li><strong>Entity conformance</strong> — types meant to be App Intent entities actually conforming correctly, with the required identifiers and display representations.</li><li><strong>Parameter wrappers</strong> — parameters using the correct <code>@Parameter</code> wrappers and metadata rather than plain properties.</li><li><strong>Metadata protocols</strong> — the supporting protocol conformances the system needs to surface the intent.</li></ul><p>Because it understands the App Intents structure through the AST, it can tell a properly-wrapped parameter from a plain property, and a complete entity conformance from one that’s missing the piece that only matters at registration.</p><p>This is a niche checker with a very specific payoff: the App Intents failure mode is uniquely frustrating because the feedback loop is so long — you don’t see the problem until you build, install, and try to invoke the intent from Shortcuts or Siri. Moving that check to commit time collapses a multi-minute test cycle into an instant gate finding.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check appintents-readiness
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/specialty/mcp-readiness</guid><title>&quot;MCPReadinessAuditor: When the Tool Schema and the Code Disagree&quot;</title><link>https://www.justinpurnell.com/QualityGate/specialty/mcp-readiness</link><description><![CDATA[Model Context Protocol servers describe their tools in a schema and implement them in code. When the two drift apart, the model calls a tool that isn’t there.]]></description><pubDate>Fri, 31 Jul 2026 09:45:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[specialty]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Model Context Protocol servers describe their tools in a schema and implement them in code. When the two drift apart, the model calls a tool that isn’t there.</strong></p><hr /><p>An MCP server exposes tools to a language model: each tool has a declared schema — its name, its parameters, their types — and a corresponding implementation. The schema is a contract the model reads. If the schema advertises a parameter the implementation doesn’t handle, or the implementation expects an argument the schema never declares, the model calls the tool the way the schema promised and the call fails in a way that’s maddening to debug, because the <em>description</em> and the <em>behavior</em> disagree.</p><p><code>MCPReadinessAuditor</code> cross-references the tool schema against the implementation and flags the mismatches:</p><ul><li>A schema parameter with no corresponding handling in the implementation</li><li>An implementation reading an argument the schema never declares</li><li>Type mismatches between the declared schema and the code that consumes it</li></ul><h2>Why a checker for this</h2><p>As more of a codebase exists to be <em>called by a model</em> rather than by a human, the schema-implementation contract becomes load-bearing — and it’s exactly the kind of thing that drifts silently, because nothing forces the two to stay in sync. A human reviewer reads the schema or the code, rarely both side by side. The auditor reads both, every commit, and flags the moment they diverge — before a model discovers the gap the hard way. It’s a small, forward-looking checker for a workflow that’s increasingly common.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check mcp-readiness
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/project-health/submodule-audit</guid><title>&quot;SubmoduleAuditor: Git Submodules That Point at the Wrong Commit&quot;</title><link>https://www.justinpurnell.com/QualityGate/project-health/submodule-audit</link><description><![CDATA[Git submodules are a reliable way to ship a repo that doesn’t build for anyone else. This checker keeps them honest.]]></description><pubDate>Fri, 31 Jul 2026 09:44:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[project-health]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Git submodules are a reliable way to ship a repo that doesn’t build for anyone else. This checker keeps them honest.</strong></p><hr /><p>Submodules fail quietly. A submodule pointer references a commit that was never pushed; a submodule sits on a detached HEAD that drifted; a <code>.gitmodules</code> entry points somewhere a fresh clone can’t reach. The person who set it up has the right state locally, so it works for them — and fails for everyone who clones after. <code>SubmoduleAuditor</code> catches these before they become someone else’s confusing clone failure.</p><h2>What it checks</h2><ul><li><strong>Unpushed pointers</strong> — the superproject references a submodule commit that doesn’t exist on the remote, so a fresh clone can’t fetch it.</li><li><strong>Detached or drifted HEADs</strong> — a submodule that’s wandered off its intended ref.</li><li><strong><code>.gitmodules</code> consistency</strong> — the declared URLs and paths matching the actual submodule state.</li></ul><p>It’s a small, focused checker, and its value is entirely about <em>other people’s clones</em>. The failure mode it prevents is the worst kind of “works on my machine”: the code is correct, the tests pass, and the repo is still broken for the next person to check it out — because a pointer references a commit only your local copy has ever seen. A gate that runs on every commit catches the bad pointer while it’s still cheap to fix.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check submodule-audit
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/project-health/memory-builder</guid><title>&quot;MemoryBuilder: Memory Files Beat Documentation (for AI, Anyway)&quot;</title><link>https://www.justinpurnell.com/QualityGate/project-health/memory-builder</link><description><![CDATA[Generating and validating the project memory an AI assistant loads at session start — because documentation tells you what the code does, and memory tells the assistant what it needs to know to help.]]></description><pubDate>Fri, 31 Jul 2026 09:43:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[project-health]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Generating and validating the project memory an AI assistant loads at session start — because documentation tells you what the code does, and memory tells the assistant what it needs to know to help.</strong></p><hr /><p><code>MemoryBuilder</code> is a checker aimed squarely at the AI-augmented workflow. It generates structured <code>.claude/memory</code> files from the project’s actual state, and validates the ones that already exist. Six extractors pull the essentials: project profile, module architecture, conventions, active work, ADR summaries, and environment info.</p><h2>The insight that surprised me</h2><p>Traditional documentation describes what the code <em>does</em>, for a human who’ll read it linearly. Memory is different: it’s what an assistant needs to know <em>up front</em> to be useful in a fresh session — which repo, which machine, which conventions, what’s in flight — loaded before the first prompt, not discovered halfway through.</p><p>This turned out to be more valuable than the documentation. An assistant that starts a session already knowing the project’s conventions, the branch it’s on, and the work in progress makes far fewer of the wrong-repo, wrong-convention mistakes that otherwise eat the first twenty minutes of every session.</p><h2>Regenerate-safe</h2><p>Generated files carry frontmatter tags so they can be safely regenerated without destroying hand-written memory. The extractors refresh the machine-derivable facts (architecture, profile, environment) while leaving the human-authored notes (feedback, judgment calls) untouched. The result is memory that stays current without a person maintaining it — which is the only kind of documentation that actually stays current.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check memory-builder
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/project-health/swift-version</guid><title>&quot;SwiftVersionChecker: Toolchain Drift, Caught Early&quot;</title><link>https://www.justinpurnell.com/QualityGate/project-health/swift-version</link><description><![CDATA[swift-tools-version validation and upgrade feasibility — because “works on my machine” often means “works on my toolchain.”]]></description><pubDate>Fri, 31 Jul 2026 09:42:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[project-health]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong><code>swift-tools-version</code> validation and upgrade feasibility — because “works on my machine” often means “works on my toolchain.”</strong></p><hr /><p>Swift toolchains drift. Your laptop is on 6.4; the build server is on 6.3.3; CI is on something else again. A <code>Package.swift</code> that declares a <code>swift-tools-version</code> newer than a target machine’s toolchain fails to resolve there — and the failure often looks like a mysterious dependency error rather than “your toolchain is too old.” <code>SwiftVersionChecker</code> validates the declared tools version and assesses whether an upgrade is feasible before that mismatch bites.</p><h2>Why it matters more than it sounds</h2><p>I run into this constantly: local Swift 6.4, a production server on 6.3.3, and features that build fine in one place and not the other. The rule of the workflow is to verify version compatibility <em>first</em>, before any deploy — because discovering a toolchain mismatch after you’ve shipped is expensive, and discovering it at commit time is free.</p><p>The checker keeps the declared <code>swift-tools-version</code> honest against what the project actually needs, and flags when a bump is being proposed that a target environment can’t yet support. It’s a small guardrail against a specific, recurring source of “it built for me” — the kind of drift that’s invisible until the one machine that matters can’t resolve the package.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check swift-version
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/project-health/release-readiness</guid><title>&quot;ReleaseReadinessAuditor: The Placeholder You Forgot to Fill In&quot;</title><link>https://www.justinpurnell.com/QualityGate/project-health/release-readiness</link><description><![CDATA[Missing changelog entries, README placeholders, TODO/FIXME pending-work markers — the small omissions that turn into embarrassing releases.]]></description><pubDate>Fri, 31 Jul 2026 09:41:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[project-health]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Missing changelog entries, README placeholders, <code>TODO</code>/<code>FIXME</code> pending-work markers — the small omissions that turn into embarrassing releases.</strong></p><hr /><p>Every release has a checklist, and every checklist has the item everyone forgets. <code>ReleaseReadinessAuditor</code> is that checklist as code: it flags the artifacts that say “not actually ready” even when the tests are green.</p><h2>What it catches</h2><ul><li><strong>Missing CHANGELOG entries</strong> — a batch of commits with nothing recorded in the changelog. The project’s own rule pairs a feature commit with its changelog entry; this enforces it.</li><li><strong>README placeholders</strong> — the <code>TODO: description here</code>, the <code><!-- fill in --></code>, the lorem-ipsum paragraph that shipped because nobody re-read the README before tagging.</li><li><strong>Pending-work markers</strong> — <code>TODO</code>, <code>FIXME</code>, <code>XXX</code> left in code that’s about to be released, so “I’ll come back to this” doesn’t silently become “this shipped.”</li></ul><p>None of these are correctness bugs. They’re the difference between a release that looks finished and one that looks abandoned — and they’re exactly the things a human reviewer skims past because they’re focused on the code. A gate doesn’t skim.</p><p>The philosophy running through the whole project applies here too: the checklist that lives in a document gets skipped under deadline pressure; the checklist that’s a gate runs every time, including the time you’re in a hurry — which is precisely when you’d otherwise ship the placeholder.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check release-readiness
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/project-health/dependency-audit</guid><title>&quot;DependencyAuditor: Catching the Import That Doesn&apos;t Exist&quot;</title><link>https://www.justinpurnell.com/QualityGate/project-health/dependency-audit</link><description><![CDATA[Package.resolved sync, branch pins, local overrides — and a rule made for the AI era: the hallucinated import.]]></description><pubDate>Fri, 31 Jul 2026 09:40:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[project-health]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong><code>Package.resolved</code> sync, branch pins, local overrides — and a rule made for the AI era: the hallucinated import.</strong></p><hr /><p><code>DependencyAuditor</code> keeps a package’s dependency story honest. It checks that <code>Package.resolved</code> is in sync, flags branch pins and local path overrides that shouldn’t ship, and — the rule I reach for most now — detects <strong>hallucinated imports.</strong></p><h2>The hallucinated import</h2><p>An AI assistant writing Swift will confidently <code>import SomeModule</code> that doesn’t exist — a plausible-sounding name it inferred rather than a real dependency. It compiles in the assistant’s head and fails on your machine, sometimes with a confusing error far from the import. As more code is AI-drafted, this stops being rare.</p><p>The auditor parses every <code>Package.swift</code> manifest (via the AST, not regex) to build the set of modules the project <em>actually</em> declares, then checks every <code>import</code> against it:</p><pre><code class="language-swift">import Foundation        // ✅ system
import BusinessMath      // ✅ declared in Package.swift
import QualityMetrics    // ❌ imported but declared nowhere — hallucinated?
</code></pre><p>Because it reads the manifests structurally, it works in monorepos with no root <code>Package.swift</code> — it discovers every manifest recursively and unions their targets. An import that resolves to none of them is flagged before the build fails somewhere less obvious.</p><p>This is a small rule with an outsized payoff in an AI-augmented workflow: it catches a specific, increasingly-common class of “code that looks right and isn’t” at the earliest possible moment.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check dependency-audit
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/project-health/status</guid><title>&quot;StatusAuditor: When the Docs and the Code Disagree&quot;</title><link>https://www.justinpurnell.com/QualityGate/project-health/status</link><description><![CDATA[Drift between what your project documents claim and what the code actually is — with a verdict that can change even when the code doesn’t.]]></description><pubDate>Fri, 31 Jul 2026 09:39:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[project-health]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Drift between what your project documents claim and what the code actually is — with a verdict that can change even when the code doesn’t.</strong></p><hr /><p>Project status docs rot. The roadmap says a feature is “in progress” months after it shipped; the architecture doc describes a module that was deleted; the changelog stops at last quarter. <code>StatusAuditor</code> compares the project’s status documents against its actual code state and flags the drift — and it supports <code>--fix</code> to reconcile the obvious cases.</p><h2>The unusual part: an out-of-tree verdict</h2><p>Most checkers are pure functions of the source: same commit, same result. <code>StatusAuditor</code> is different, and deliberately so. Its verdict depends on things <em>outside</em> the working tree — the project’s Master Plan (in a separate repo) and a <strong>90-day staleness</strong> window. That means a completely static commit can flip from pass to fail simply because time passed or the plan changed elsewhere.</p><p>That’s not a bug; it’s the point. “Is this project’s stated status still true?” is a question the code alone can’t answer. A module that was accurate three months ago may be stale today with no line changed. By pulling in the external plan and the clock, the auditor catches the drift that a source-only checker structurally cannot.</p><h2>Bootstrapping</h2><p>For a project with no status docs yet, <code>quality-gate --check status --bootstrap</code> generates the initial set from the project’s actual state — so the baseline the auditor checks against reflects reality on day one, not an aspirational template.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check status
quality-gate --check status --bootstrap    # generate initial status docs
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/project-health/test</guid><title>&quot;TestRunner: The Flip Detector That Catches Races a Passing Suite Hides&quot;</title><link>https://www.justinpurnell.com/QualityGate/project-health/test</link><description><![CDATA[It runs your tests — and then does two things a normal test run can’t: it remembers, and it provokes.]]></description><pubDate>Fri, 31 Jul 2026 09:38:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[project-health]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>It runs your tests — and then does two things a normal test run can’t: it remembers, and it provokes.</strong></p><hr /><p><code>TestRunner</code> wraps <code>swift test</code>, parsing both Swift Testing and XCTest results. That’s the table stakes. The interesting part is what it does <em>around</em> the run, and it comes straight from the <a href="https://www.justinpurnell.com/QualityGate/correctness/concurrency">Narbis stop-vs-completed race</a>.</p><h2>The flip detector — it remembers</h2><p>A gate runs the suite on every commit, but each run is otherwise memoryless. It can’t tell “passed last time, fails now, no source change” from “always fails.” The flip detector fixes that: it persists a per-package pass/fail roster and, on the next run, flags any test whose outcome <strong>flipped while the package fingerprint was unchanged.</strong></p><p>A flip on byte-identical source is a <em>definitive</em> signal of scheduler-dependent behavior — a race — not a code change. This is exactly the class the Narbis bug was: the two-consecutive-clean-runs flake policy correctly passed it (it <em>was</em> clean twice), but a single flip against an identical package is the alarm. The diagnostic names both commits, so the regression window is bounded, and it’s framed “scheduler-dependent behavior — find the window,” not “flaky test.”</p><h2>Stress mode — it provokes</h2><p>The flip detector is <em>passive</em>: it waits for a race to surface. Stress mode <em>provokes</em> one. Tests carrying a <code>// TIMING:</code> marker are self-identifying race candidates. On demand, the runner re-runs <em>only</em> those tests N times — optionally under a background CPU-contention harness sized to <code>cores − 1</code> — and flags any test that wasn’t unanimous across the identical runs. Since every run shares one commit and one source tree, a non-unanimous outcome is a <em>definitive</em> race, stronger than a cross-commit flip.</p><p>The original Narbis race surfaced only when parallel package gates <em>accidentally</em> stress-loaded the scheduler. Stress mode makes that deliberate — and reproducible.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check test
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/project-health/build</guid><title>&quot;BuildChecker: The Compiler Is a Checker Too&quot;</title><link>https://www.justinpurnell.com/QualityGate/project-health/build</link><description><![CDATA[The simplest checker in the suite, and the one everything else depends on: does it build, with zero warnings?]]></description><pubDate>Fri, 31 Jul 2026 09:37:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[project-health]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>The simplest checker in the suite, and the one everything else depends on: does it build, with zero warnings?</strong></p><hr /><p><code>BuildChecker</code> wraps <code>swift build</code> and captures every compiler error and warning as gate diagnostics. It sounds trivial, and mechanically it is — but it encodes a policy that isn’t trivial at all: <strong>a warning is a failure.</strong></p><p>Most teams let warnings accumulate. First there are three, then thirty, then three hundred, and the signal is gone — nobody reads a build log with three hundred warnings, so the one that matters hides in the noise. quality-gate-swift runs with strict concurrency (<code>-strict-concurrency=complete</code>) and treats the whole thing as pass/fail: zero errors, zero warnings, or the commit doesn’t land.</p><p>The value is compounding. Because the count is always zero, a <em>new</em> warning is immediately visible — it’s the difference between 0 and 1, not 300 and 301. Keeping the number at zero is what makes the number meaningful.</p><p>Because it locks the SwiftPM build directory, <code>BuildChecker</code> runs sequentially, outside the parallel checker group — one of the few that opts out of concurrency. It’s usually the first thing to run and the foundation for everything after it: there’s no point auditing code that doesn’t compile.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check build
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/documentation/doc-coverage</guid><title>&quot;DocCoverageChecker: Undocumented Public APIs, Ranked by How Much They Matter&quot;</title><link>https://www.justinpurnell.com/QualityGate/documentation/doc-coverage</link><description><![CDATA[Every public symbol is a promise to a future reader. This checker makes sure the promise comes with an explanation.]]></description><pubDate>Fri, 31 Jul 2026 09:36:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[documentation]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Every <code>public</code> symbol is a promise to a future reader. This checker makes sure the promise comes with an explanation.</strong></p><hr /><p>A <code>public</code> API with no doc comment is a small debt: someone, someday, has to reverse-engineer what it does from its name and signature. <code>DocCoverageChecker</code> flags undocumented public symbols — and it’s precise about which ones actually count.</p><h2>What it catches, and what it doesn’t</h2><pre><code class="language-swift">// ❌ undocumented public API
public func calibrate(_ input: Signal) -> Score { … }

// ✅ documented
/// Calibrates a raw signal into a normalized score in 0...1.
public func calibrate(_ input: Signal) -> Score { … }
</code></pre><p>It’s smarter than “does a <code>///</code> exist above this line.” It detects <strong>inherited documentation</strong> — a protocol requirement’s doc carries to conformances, so you’re not forced to repeat yourself — and it <strong>ranks by usage priority</strong>, so the public API that everything depends on is flagged louder than a rarely-touched one. Documentation effort is finite; the checker points it where it pays off most.</p><h2>The dogfooding moment I keep re-living</h2><p>This is the checker that catches <em>me</em> the most. Twice this session, building the compliance-mapping layer, I shipped a batch of public types with no docs — and the gate flagged nine of them, in a project whose own doc-coverage checker exists to flag exactly that. Both times I documented them at the root rather than lowering the bar. A checker that flags undocumented public APIs is worthless if its own author gets a pass; the point is that nobody does, including the person who wrote the checker.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check doc-coverage
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/hygiene/duplication</guid><title>&quot;DuplicationAuditor: From 14,000 False Positives to 11 Real Clones&quot;</title><link>https://www.justinpurnell.com/QualityGate/hygiene/duplication</link><description><![CDATA[Copy-paste detection that took a 14,000-finding first run down to 11 genuine clones — the precision story, again.]]></description><pubDate>Fri, 31 Jul 2026 09:35:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[hygiene]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Copy-paste detection that took a 14,000-finding first run down to 11 genuine clones — the precision story, again.</strong></p><hr /><p>Clone detection is where a static tool most easily embarrasses itself. Normalize too aggressively and every function that shares Swift’s grammar looks like a duplicate; normalize too little and a renamed copy slips through. <code>DuplicationAuditor</code> uses normalized token-stream sliding-window hashing with a deterministic FNV-1a hash (never <code>String.hashValue</code>, which isn’t stable across runs).</p><h2>The 14,000 → 11 story</h2><p>Dogfooded against quality-gate-swift itself, the first version emitted <strong>14,000+</strong> findings. Almost all were false positives: the tokenizer collapsed <em>all</em> literal content to a single <code>LIT</code> sentinel, so an ADR-YAML test and a <code>Package.swift</code> test — completely different data — reduced to a byte-identical token stream and “matched.”</p><p>Four fixes, on two axes:</p><ul><li><strong>Precision.</strong> Normalize only <em>identifiers</em> (→ <code>ID</code>) and keep literal content <strong>verbatim</strong> — so a genuine copy (which preserves its literals) is distinguished from isomorphic-but-different code that merely shares the grammar. A diversity floor drops low-variety boilerplate; the minimum-token threshold rose; tests are excluded by default (parallel test structure is <em>good</em> design).</li><li><strong>Presentation.</strong> Report one diagnostic per <strong>clone class</strong> — all the maximal blocks sharing a normalized sequence, grouped into one row (<code>222-token clone across 3 sites: A ≈ B ≈ C</code>) — instead of <code>N·(N−1)/2</code> pairwise rows.</li></ul><p>Result: 14,000+ rows collapsed to <strong>11</strong>, each a genuine 189–405-token clone (real auditor scaffolding worth refactoring), zero test-boilerplate noise.</p><h2>The point, one more time</h2><p>This is the whole thesis in a single checker. The value wasn’t the clever hashing — it was the days spent making the output <em>trustworthy</em>. Advisory by default (<code>.note</code> unless you escalate), because whether a clone is worth extracting is a judgment call. But an advisory tool that produces 14,000 findings gets ignored; one that produces 11 real ones gets acted on.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check duplication
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/hygiene/smells</guid><title>&quot;SmellPack: God Objects, Deep Nesting, and Other Code Smells&quot;</title><link>https://www.justinpurnell.com/QualityGate/hygiene/smells</link><description><![CDATA[The generalist advisory suite: parameter counts, nesting depth, god objects, over-long types and closures — measured, stated, never enforced.]]></description><pubDate>Fri, 31 Jul 2026 09:34:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[hygiene]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>The generalist advisory suite: parameter counts, nesting depth, god objects, over-long types and closures — measured, stated, never enforced.</strong></p><hr /><p><code>SmellPack</code> is the catch-all for the metrics that don’t belong to any single correctness rule but still tell you something true about maintainability. Every finding is a note that states the measured value and the threshold — no drama, just a measurement you can act on or ignore.</p><h2>What it measures</h2><pre><code>smell.parameter-count   — a function/init with too many parameters
smell.nesting-depth     — statement nesting inside one function body
smell.god-object        — a type with too many members (same-file extensions counted)
smell.type-length       — a type body spanning too many lines
smell.closure-length    — an over-long closure body
</code></pre><p>The god-object rule is the one I find most useful: it counts a type’s members <em>including its same-file extensions</em>, because splitting a 400-line class across three extensions in the same file doesn’t make it smaller — it just hides the size. Every metric flags strictly <em>over</em> its threshold, so a measurement exactly at the line is quiet, and every threshold is config-tunable.</p><h2>Advisory by contract</h2><p><code>SmellPack</code> always reports <code>.passed</code>, whatever it finds. It’s advisory by design, matching the <a href="https://www.justinpurnell.com/QualityGate/correctness/complexity">complexity</a> and <a href="https://www.justinpurnell.com/QualityGate/correctness/legibility">legibility</a> precedent: smells <em>inform</em>, they never gate. A <code>// smell:exempt</code> on the flagged declaration’s line is recorded as an override — never silently dropped. The point isn’t to enforce a member count; it’s to make the shape visible so refactoring is a decision, not an accident.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check smells
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/hygiene/idiom</guid><title>&quot;IdiomAuditor: SwiftLint&apos;s Best Rules, Native and AST-Based&quot;</title><link>https://www.justinpurnell.com/QualityGate/hygiene/idiom</link><description><![CDATA[The head of SwiftLint’s usage, rebuilt as native AST rules — so you can drop a tool instead of adding one.]]></description><pubDate>Fri, 31 Jul 2026 09:33:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[hygiene]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>The head of SwiftLint’s usage, rebuilt as native AST rules — so you can drop a tool instead of adding one.</strong></p><hr /><p>The goal here was substitutional, not additive: adopting quality-gate should mean <em>dropping</em> a tool, not running one more. <code>IdiomAuditor</code> reimplements the most-used SwiftLint rules — about twenty of them — as native SwiftSyntax checks, so the style layer lives in the same gate as the correctness layer.</p><h2>The kinds of rule</h2><p>Empty-count (<code>x.count == 0</code> → <code>x.isEmpty</code>), the redundant-syntax family, syntactic sugar (<code>Array<Int></code> → <code>[Int]</code>), shorthand operators, identifier and type naming, TODO-ticket policy, file/function/line length, the whitespace family, unused closure parameters, implicit getters, redundant <code>String</code> enum values, legacy <code>random</code>, <code>contains</code>-over-<code>first</code>.</p><pre><code class="language-swift">// ❌ if array.count == 0        ✅ if array.isEmpty
// ❌ let x: Array<Int> = []      ✅ let x: [Int] = []
// ❌ x = x + 1                   ✅ x += 1
</code></pre><h2>Advisory, with safe autofix</h2><p>Idiom rules are <strong>advisory</strong> — notes by default, because the gate blocks on correctness, not whitespace (you can escalate them in config if you want). Many carry a safe-subset <code>suggestedFix</code>, verified by round-trip tests: apply the fix, reparse, confirm the rule now stays silent. And there’s a migration path — <code>quality-gate import-swiftlint</code> reads your <code>.swiftlint.yml</code>, maps enabled rules to their native equivalents (thresholds carried over), translates <code>custom_rules</code> verbatim, and prints an honest report of what it <em>couldn’t</em> map. Even the migration tells the truth about its gaps.</p><p><code>// idiom:exempt</code> suppresses a site — recorded as an override, never silent.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check idiom
quality-gate import-swiftlint      # migrate an existing .swiftlint.yml
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/hygiene/accessibility</guid><title>&quot;AccessibilityAuditor: The Users You Forgot to Test With&quot;</title><link>https://www.justinpurnell.com/QualityGate/hygiene/accessibility</link><description><![CDATA[Missing labels, fixed font sizes, color-only meaning — the accessibility defects that never show up in your own testing, because you’re not the user who hits them.]]></description><pubDate>Fri, 31 Jul 2026 09:32:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[hygiene]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Missing labels, fixed font sizes, color-only meaning — the accessibility defects that never show up in your own testing, because you’re not the user who hits them.</strong></p><hr /><p>Accessibility bugs are uniquely easy to ship: they’re invisible to the person who wrote the code and tested it with their own eyes and their own default settings. <code>AccessibilityAuditor</code> catches the common SwiftUI shapes at commit time, before they reach someone using VoiceOver, Dynamic Type, or Differentiate Without Color.</p><h2>What it catches</h2><pre><code class="language-swift">// ❌ an image conveying meaning with no accessibility label
Image("chart-up")
// ✅ Image("chart-up").accessibilityLabel("Revenue up 12%")

// ❌ a fixed font size — ignores Dynamic Type
Text("Total").font(.system(size: 14))
// ✅ Text("Total").font(.body)

// ❌ meaning by color alone
Circle().fill(isError ? .red : .green)          // invisible to color-blind users
// ✅ pair color with a shape, label, or icon

// ❌ a decorative image not hidden from assistive tech
// ✅ Image("divider").accessibilityHidden(true)
</code></pre><p>It also flags custom tap controls without an accessibility label or hint, and <code>onTapGesture</code> used where a <code>Button</code> (with its built-in traits) belongs. Because it’s SwiftUI-structure-aware, it can tell a labeled image from an unlabeled one, and a decorative image correctly marked <code>accessibilityHidden</code> from one that was just forgotten.</p><p>Alongside the <a href="https://www.justinpurnell.com/QualityGate/safety-security/hig-auditor">HIGAuditor</a> and <a href="https://www.justinpurnell.com/QualityGate/hygiene/context">ContextAuditor</a>, this is the part of the gate that insists “correct” includes <em>correct for the person using it</em> — a standard the compiler will never enforce for you.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check accessibility
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/hygiene/context</guid><title>&quot;ContextAuditor: Consent, Surveillance, and the Ethics of Code&quot;</title><link>https://www.justinpurnell.com/QualityGate/hygiene/context</link><description><![CDATA[If we can detect institutional patterns in override decisions, can we detect ethical patterns in the code itself? This is that checker.]]></description><pubDate>Fri, 31 Jul 2026 09:31:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[hygiene]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>If we can detect institutional patterns in override decisions, can we detect ethical patterns in the code itself? This is that checker.</strong></p><hr /><p><code>ContextAuditor</code> came directly out of building the <a href="https://www.justinpurnell.com/QualityGate/mirror/the-mirror">Institutional Judgment System</a>. If the whole project is about making judgment visible, the obvious next question is whether the <em>code</em> encodes judgments about the people it touches — and whether those judgments are made deliberately or by omission.</p><h2>Four categories of ethical risk</h2><ul><li><strong>Missing consent guards</strong> — accessing location, contacts, camera, health data, calendar, or photos with no prior consent check in the enclosing function.</li><li><strong>Unguarded analytics</strong> — tracking user behavior with no opt-out guard.</li><li><strong>Automated decisions without review</strong> — an ML prediction feeding directly into a deny / block / suspend action with no human-review step.</li><li><strong>Surveillance patterns</strong> — background location tracking with no disclosure annotation.</li></ul><pre><code class="language-swift">// ❌ accessing health data with no consent check
let steps = try await healthStore.samples(for: .stepCount)
// ✅ // CONSENT: user opted in via Settings > Privacy
guard hasHealthConsent else { return }
let steps = try await healthStore.samples(for: .stepCount)
</code></pre><p>Each rule is suppressed with a <em>justification</em> annotation — <code>// CONSENT: User opted in via Settings > Privacy</code>. The requirement for justification text is deliberate: <strong>silent suppression is not allowed.</strong> An access that’s genuinely consented is recorded as such; an access that isn’t gets a finding.</p><h2>Earn your keep or get removed</h2><p><code>ContextAuditor</code> is a separate, easily-disableable module, and that’s a design statement. If its findings are consistently ignored, the correct response is to <em>disable it</em> — not to force compliance. An ignored checker is worse than no checker, because it trains people to skip the gate’s output. This is the “flag to understand, not forbid” philosophy in its sharpest form: an advisory ethical checker has to earn its place, every run, or it should be gone.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check context
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item></channel></rss>