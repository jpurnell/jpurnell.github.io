<?xml version="1.0" encoding="UTF-8" ?><rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Justin Purnell</title><description>Justin Purnell — Founder of Ledge Partners. Former Goldman Sachs credit analyst, Head of Product at Hotels at Home, VP at NBCUniversal. Princeton &apos;00, Tuck MBA.</description><link>https://www.justinpurnell.com</link><atom:link
    href="https://www.justinpurnell.com/feed.rss"
    rel="self" type="application/rss+xml"
/><language>en</language><generator>Ignite v0.6.0</generator><image><url>https://www.justinpurnell.com/images/logos/rss.png</url><title>Justin Purnell</title><link>https://www.justinpurnell.com</link><width>144</width><height>152</height></image><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/hygiene/duplication</guid><title>&quot;DuplicationAuditor: From 14,000 False Positives to 11 Real Clones&quot;</title><link>https://www.justinpurnell.com/QualityGate/hygiene/duplication</link><description><![CDATA[Copy-paste detection that took a 14,000-finding first run down to 11 genuine clones — the precision story, again.]]></description><pubDate>Mon, 24 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[hygiene]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Copy-paste detection that took a 14,000-finding first run down to 11 genuine clones — the precision story, again.</strong></p><hr /><p>Clone detection is where a static tool most easily embarrasses itself. Normalize too aggressively and every function that shares Swift’s grammar looks like a duplicate; normalize too little and a renamed copy slips through. <code>DuplicationAuditor</code> uses normalized token-stream sliding-window hashing with a deterministic FNV-1a hash (never <code>String.hashValue</code>, which isn’t stable across runs).</p><h2>The 14,000 → 11 story</h2><p>Dogfooded against quality-gate-swift itself, the first version emitted <strong>14,000+</strong> findings. Almost all were false positives: the tokenizer collapsed <em>all</em> literal content to a single <code>LIT</code> sentinel, so an ADR-YAML test and a <code>Package.swift</code> test — completely different data — reduced to a byte-identical token stream and “matched.”</p><p>Four fixes, on two axes:</p><ul><li><strong>Precision.</strong> Normalize only <em>identifiers</em> (→ <code>ID</code>) and keep literal content <strong>verbatim</strong> — so a genuine copy (which preserves its literals) is distinguished from isomorphic-but-different code that merely shares the grammar. A diversity floor drops low-variety boilerplate; the minimum-token threshold rose; tests are excluded by default (parallel test structure is <em>good</em> design).</li><li><strong>Presentation.</strong> Report one diagnostic per <strong>clone class</strong> — all the maximal blocks sharing a normalized sequence, grouped into one row (<code>222-token clone across 3 sites: A ≈ B ≈ C</code>) — instead of <code>N·(N−1)/2</code> pairwise rows.</li></ul><p>Result: 14,000+ rows collapsed to <strong>11</strong>, each a genuine 189–405-token clone (real auditor scaffolding worth refactoring), zero test-boilerplate noise.</p><h2>The point, one more time</h2><p>This is the whole thesis in a single checker. The value wasn’t the clever hashing — it was the days spent making the output <em>trustworthy</em>. Advisory by default (<code>.note</code> unless you escalate), because whether a clone is worth extracting is a judgment call. But an advisory tool that produces 14,000 findings gets ignored; one that produces 11 real ones gets acted on.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check duplication
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/hygiene/smells</guid><title>&quot;SmellPack: God Objects, Deep Nesting, and Other Code Smells&quot;</title><link>https://www.justinpurnell.com/QualityGate/hygiene/smells</link><description><![CDATA[The generalist advisory suite: parameter counts, nesting depth, god objects, over-long types and closures — measured, stated, never enforced.]]></description><pubDate>Sun, 23 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[hygiene]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>The generalist advisory suite: parameter counts, nesting depth, god objects, over-long types and closures — measured, stated, never enforced.</strong></p><hr /><p><code>SmellPack</code> is the catch-all for the metrics that don’t belong to any single correctness rule but still tell you something true about maintainability. Every finding is a note that states the measured value and the threshold — no drama, just a measurement you can act on or ignore.</p><h2>What it measures</h2><pre><code>smell.parameter-count   — a function/init with too many parameters
smell.nesting-depth     — statement nesting inside one function body
smell.god-object        — a type with too many members (same-file extensions counted)
smell.type-length       — a type body spanning too many lines
smell.closure-length    — an over-long closure body
</code></pre><p>The god-object rule is the one I find most useful: it counts a type’s members <em>including its same-file extensions</em>, because splitting a 400-line class across three extensions in the same file doesn’t make it smaller — it just hides the size. Every metric flags strictly <em>over</em> its threshold, so a measurement exactly at the line is quiet, and every threshold is config-tunable.</p><h2>Advisory by contract</h2><p><code>SmellPack</code> always reports <code>.passed</code>, whatever it finds. It’s advisory by design, matching the <a href="https://www.justinpurnell.com/QualityGate/correctness/complexity">complexity</a> and <a href="https://www.justinpurnell.com/QualityGate/correctness/legibility">legibility</a> precedent: smells <em>inform</em>, they never gate. A <code>// smell:exempt</code> on the flagged declaration’s line is recorded as an override — never silently dropped. The point isn’t to enforce a member count; it’s to make the shape visible so refactoring is a decision, not an accident.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check smells
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/hygiene/idiom</guid><title>&quot;IdiomAuditor: SwiftLint&apos;s Best Rules, Native and AST-Based&quot;</title><link>https://www.justinpurnell.com/QualityGate/hygiene/idiom</link><description><![CDATA[The head of SwiftLint’s usage, rebuilt as native AST rules — so you can drop a tool instead of adding one.]]></description><pubDate>Sat, 22 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[hygiene]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>The head of SwiftLint’s usage, rebuilt as native AST rules — so you can drop a tool instead of adding one.</strong></p><hr /><p>The goal here was substitutional, not additive: adopting quality-gate should mean <em>dropping</em> a tool, not running one more. <code>IdiomAuditor</code> reimplements the most-used SwiftLint rules — about twenty of them — as native SwiftSyntax checks, so the style layer lives in the same gate as the correctness layer.</p><h2>The kinds of rule</h2><p>Empty-count (<code>x.count == 0</code> → <code>x.isEmpty</code>), the redundant-syntax family, syntactic sugar (<code>Array<Int></code> → <code>[Int]</code>), shorthand operators, identifier and type naming, TODO-ticket policy, file/function/line length, the whitespace family, unused closure parameters, implicit getters, redundant <code>String</code> enum values, legacy <code>random</code>, <code>contains</code>-over-<code>first</code>.</p><pre><code class="language-swift">// ❌ if array.count == 0        ✅ if array.isEmpty
// ❌ let x: Array<Int> = []      ✅ let x: [Int] = []
// ❌ x = x + 1                   ✅ x += 1
</code></pre><h2>Advisory, with safe autofix</h2><p>Idiom rules are <strong>advisory</strong> — notes by default, because the gate blocks on correctness, not whitespace (you can escalate them in config if you want). Many carry a safe-subset <code>suggestedFix</code>, verified by round-trip tests: apply the fix, reparse, confirm the rule now stays silent. And there’s a migration path — <code>quality-gate import-swiftlint</code> reads your <code>.swiftlint.yml</code>, maps enabled rules to their native equivalents (thresholds carried over), translates <code>custom_rules</code> verbatim, and prints an honest report of what it <em>couldn’t</em> map. Even the migration tells the truth about its gaps.</p><p><code>// idiom:exempt</code> suppresses a site — recorded as an override, never silent.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check idiom
quality-gate import-swiftlint      # migrate an existing .swiftlint.yml
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/hygiene/accessibility</guid><title>&quot;AccessibilityAuditor: The Users You Forgot to Test With&quot;</title><link>https://www.justinpurnell.com/QualityGate/hygiene/accessibility</link><description><![CDATA[Missing labels, fixed font sizes, color-only meaning — the accessibility defects that never show up in your own testing, because you’re not the user who hits them.]]></description><pubDate>Fri, 21 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[hygiene]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Missing labels, fixed font sizes, color-only meaning — the accessibility defects that never show up in your own testing, because you’re not the user who hits them.</strong></p><hr /><p>Accessibility bugs are uniquely easy to ship: they’re invisible to the person who wrote the code and tested it with their own eyes and their own default settings. <code>AccessibilityAuditor</code> catches the common SwiftUI shapes at commit time, before they reach someone using VoiceOver, Dynamic Type, or Differentiate Without Color.</p><h2>What it catches</h2><pre><code class="language-swift">// ❌ an image conveying meaning with no accessibility label
Image("chart-up")
// ✅ Image("chart-up").accessibilityLabel("Revenue up 12%")

// ❌ a fixed font size — ignores Dynamic Type
Text("Total").font(.system(size: 14))
// ✅ Text("Total").font(.body)

// ❌ meaning by color alone
Circle().fill(isError ? .red : .green)          // invisible to color-blind users
// ✅ pair color with a shape, label, or icon

// ❌ a decorative image not hidden from assistive tech
// ✅ Image("divider").accessibilityHidden(true)
</code></pre><p>It also flags custom tap controls without an accessibility label or hint, and <code>onTapGesture</code> used where a <code>Button</code> (with its built-in traits) belongs. Because it’s SwiftUI-structure-aware, it can tell a labeled image from an unlabeled one, and a decorative image correctly marked <code>accessibilityHidden</code> from one that was just forgotten.</p><p>Alongside the <a href="https://www.justinpurnell.com/QualityGate/safety-security/hig-auditor">HIGAuditor</a> and <a href="https://www.justinpurnell.com/QualityGate/hygiene/context">ContextAuditor</a>, this is the part of the gate that insists “correct” includes <em>correct for the person using it</em> — a standard the compiler will never enforce for you.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check accessibility
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/hygiene/context</guid><title>&quot;ContextAuditor: Consent, Surveillance, and the Ethics of Code&quot;</title><link>https://www.justinpurnell.com/QualityGate/hygiene/context</link><description><![CDATA[If we can detect institutional patterns in override decisions, can we detect ethical patterns in the code itself? This is that checker.]]></description><pubDate>Thu, 20 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[hygiene]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>If we can detect institutional patterns in override decisions, can we detect ethical patterns in the code itself? This is that checker.</strong></p><hr /><p><code>ContextAuditor</code> came directly out of building the <a href="https://www.justinpurnell.com/QualityGate/mirror/the-mirror">Institutional Judgment System</a>. If the whole project is about making judgment visible, the obvious next question is whether the <em>code</em> encodes judgments about the people it touches — and whether those judgments are made deliberately or by omission.</p><h2>Four categories of ethical risk</h2><ul><li><strong>Missing consent guards</strong> — accessing location, contacts, camera, health data, calendar, or photos with no prior consent check in the enclosing function.</li><li><strong>Unguarded analytics</strong> — tracking user behavior with no opt-out guard.</li><li><strong>Automated decisions without review</strong> — an ML prediction feeding directly into a deny / block / suspend action with no human-review step.</li><li><strong>Surveillance patterns</strong> — background location tracking with no disclosure annotation.</li></ul><pre><code class="language-swift">// ❌ accessing health data with no consent check
let steps = try await healthStore.samples(for: .stepCount)
// ✅ // CONSENT: user opted in via Settings > Privacy
guard hasHealthConsent else { return }
let steps = try await healthStore.samples(for: .stepCount)
</code></pre><p>Each rule is suppressed with a <em>justification</em> annotation — <code>// CONSENT: User opted in via Settings > Privacy</code>. The requirement for justification text is deliberate: <strong>silent suppression is not allowed.</strong> An access that’s genuinely consented is recorded as such; an access that isn’t gets a finding.</p><h2>Earn your keep or get removed</h2><p><code>ContextAuditor</code> is a separate, easily-disableable module, and that’s a design statement. If its findings are consistently ignored, the correct response is to <em>disable it</em> — not to force compliance. An ignored checker is worse than no checker, because it trains people to skip the gate’s output. This is the “flag to understand, not forbid” philosophy in its sharpest form: an advisory ethical checker has to earn its place, every run, or it should be gone.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check context
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/hygiene/test-quality</guid><title>&quot;TestQualityAuditor: Tests That Don&apos;t Actually Test Anything&quot;</title><link>https://www.justinpurnell.com/QualityGate/hygiene/test-quality</link><description><![CDATA[A green test suite is only reassuring if the tests assert something real. This checker audits the tests themselves.]]></description><pubDate>Wed, 19 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[hygiene]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>A green test suite is only reassuring if the tests assert something real. This checker audits the tests themselves.</strong></p><hr /><p>The most dangerous test is the one that passes for the wrong reason. It runs, it’s green, and it checks nothing — a false sense of safety that’s worse than an honest gap, because nobody goes looking for coverage that appears to exist.</p><h2>What it catches</h2><pre><code class="language-swift">// ❌ a test with no assertion at all
func testLoad() { let r = loader.load() }        // did anything get verified?

// ❌ a weak assertion — passes on almost any value
XCTAssertNotNil(result)
XCTAssertTrue(count != 0)
// ✅ assert the specific expected value
XCTAssertEqual(count, 3)

// ❌ floating-point exact equality in a test
XCTAssertEqual(score, 0.85)
// ✅ XCTAssertEqual(score, 0.85, accuracy: 1e-6)

// ❌ unseeded randomness in a test — non-reproducible
let pick = items.randomElement()
</code></pre><p>It flags missing assertions, weak assertions (<code>!= 0</code>, <code>!= nil</code> instead of a specific expected value), floating-point exact equality, and unseeded randomness — the four ways a test quietly stops testing. The project’s rule is direct: assertions use specific expected values, not <code>!= 0</code> or <code>!= nil</code>; tests are deterministic and reproducible.</p><p>This is the checker that keeps the <a href="https://www.justinpurnell.com/QualityGate/project-health/test">flip detector and stress mode</a> honest. A suite full of assertion-free tests would flip-detect nothing, because nothing was ever being asserted. Test quality is the foundation the rest of the test tooling stands on.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check test-quality
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/hygiene/logging</guid><title>&quot;LoggingAuditor: No print(), No Silent Catches&quot;</title><link>https://www.justinpurnell.com/QualityGate/hygiene/logging</link><description><![CDATA[print() in production, errors swallowed in silence, and os.Logger calls that leak private data — the hygiene of knowing what your code did.]]></description><pubDate>Tue, 18 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[hygiene]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong><code>print()</code> in production, errors swallowed in silence, and <code>os.Logger</code> calls that leak private data — the hygiene of knowing what your code did.</strong></p><hr /><p>Logging is how you find out what happened when you weren’t watching. Bad logging hygiene means either no signal (a <code>print()</code> that vanishes in Release, an error caught and dropped) or too much (a logger call that prints a user’s private data to the device console). <code>LoggingAuditor</code> catches both ends.</p><h2>What it catches</h2><pre><code class="language-swift">// ❌ logging.print-statement — print() in production; invisible in Release
print("loaded \(user.email)")

// ❌ logging.catch-without-logging — an error caught and silently dropped
do { try save() } catch { }

// ❌ logging.silent-try — try? with no explanation
let data = try? Data(contentsOf: url)
// ✅ // silent: an unreadable cache file is simply skipped
let data = try? Data(contentsOf: url)

// ❌ logging.missing-privacy — os.Logger interpolating data with no privacy annotation
logger.info("token: \(token)")            // leaks to the console
// ✅ logger.info("token: \(token, privacy: .private)")
</code></pre><p>Representative rule IDs: <code>logging.print-statement</code>, <code>logging.catch-without-logging</code>, <code>logging.silent-try</code>, <code>logging.missing-privacy</code>, <code>logging.no-os-logger-import</code>, <code>logging.bare-logger-init</code>.</p><p>The <code>try?</code> rule is one I meet constantly in my own first-pass code — three times this project’s own gate caught me discarding an error without a <code>// silent:</code> reason, and each time the fix was to write down <em>why</em> the error was safe to drop. That’s the rule working as intended: not “never use <code>try?</code>,” but “never discard an error without saying why.” Adjacency is strict and the comment is recorded — searchable, never silent.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check logging
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/safety-security/hig-auditor</guid><title>&quot;HIGAuditor: Apple&apos;s Human Interface Guidelines, as Enforced Rules&quot;</title><link>https://www.justinpurnell.com/QualityGate/safety-security/hig-auditor</link><description><![CDATA[Apple’s Human Interface Guidelines are usually a PDF you’re supposed to remember. Here they’re SwiftUI rules the gate can check.]]></description><pubDate>Mon, 17 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[safety]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Apple’s Human Interface Guidelines are usually a PDF you’re supposed to remember. Here they’re SwiftUI rules the gate can check.</strong></p><hr /><p>The Human Interface Guidelines are excellent and almost entirely unenforced — they live in a document, and documents don’t run. <code>HIGAuditor</code> takes the checkable subset and turns it into rules that fire on your SwiftUI code at commit time. It’s cross-platform aware: what’s correct on iOS isn’t always correct on macOS, and the auditor knows the difference.</p><h2>The kinds of thing it catches</h2><ul><li><strong>Hit targets</strong> below the platform minimum — a tap target smaller than the guideline’s 44×44pt is a real usability defect, not a nitpick.</li><li><strong>Reserved keyboard shortcuts</strong> — binding a Command-key shortcut that the system already owns.</li><li><strong>Reduce-Motion / Reduce-Transparency</strong> — an <code>.animation()</code> with no <code>reduceMotion</code> check, or a <code>Material</code> background with no Reduce-Transparency guard, so the accessibility setting is silently ignored.</li><li><strong>Differentiate-Without-Color</strong> — state conveyed by color alone, with no guard for users who can’t distinguish it.</li></ul><p>Each diagnostic cites its Apple HIG basis, so the finding is a pointer into the actual guideline, not just an opinion. And because it’s SwiftUI-structure-aware (AST, not regex), it can tell a padded 44pt button from a bare 20pt frame, and a genuinely reduce-motion-guarded animation from an unguarded one.</p><p>This is where the gate crosses from “will it crash” into “is it <em>right for the person using it</em>” — the same instinct behind the <a href="https://www.justinpurnell.com/QualityGate/hygiene/accessibility">AccessibilityAuditor</a> and the <a href="https://www.justinpurnell.com/QualityGate/hygiene/context">ContextAuditor</a>. Correctness that stops at the compiler misses half of what “correct” means for software people actually touch.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check hig-auditor
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/safety-security/temporal-determinism</guid><title>&quot;TemporalDeterminismAuditor: When the Wall Clock Is a Bug&quot;</title><link>https://www.justinpurnell.com/QualityGate/safety-security/temporal-determinism</link><description><![CDATA[Wall-clock nondeterminism: simulated data stamped with .now, and tests that assert on measured elapsed time.]]></description><pubDate>Sun, 16 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[safety]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Wall-clock nondeterminism: simulated data stamped with <code>.now</code>, and tests that assert on measured elapsed time.</strong></p><hr /><p>The wall clock is the other great enemy of a reproducible test suite. Two shapes cause most of the trouble, and this checker catches both — using the AST to tell them apart from legitimate uses.</p><h2>What it catches</h2><p><strong>Simulated sources stamping <code>.now</code>.</strong> A data generator, fixture, or mock that stamps records with <code>Date.now</code> produces different output every run. That’s fine for real telemetry; it’s a bug in a <em>simulated</em> source that’s supposed to be reproducible.</p><pre><code class="language-swift">// ❌ a synthetic fixture that isn't reproducible
func makeSample() -> Sample { Sample(timestamp: .now, value: 42) }

// ✅ inject the clock so the fixture is deterministic
func makeSample(at time: Date) -> Sample { Sample(timestamp: time, value: 42) }
</code></pre><p><strong>Tests asserting on measured elapsed time.</strong> A test that does <code>let t = Date(); …; XCTAssert(Date().timeIntervalSince(t) < 0.1)</code> is asserting on how fast the <em>test machine</em> is. It passes on your laptop and fails in CI under load. The rule flags tests that assert on wall-clock elapsed time and points you at logical clocks or injected time instead.</p><h2>Precise about which rule runs where</h2><p>The checker is context-aware: a path containing <code>/Tests/</code> runs the elapsed-time-assertion rule; production paths run the simulated-source rule. And it doesn’t flag <em>its own</em> timing — the auditor measures its runtime with <code>ContinuousClock</code>, which is exactly the right, non-flagged way to time an operation. It flags the patterns that make behavior <em>non-reproducible</em>, not every mention of time.</p><p>Together with <a href="https://www.justinpurnell.com/QualityGate/safety-security/stochastic-determinism">StochasticDeterminismAuditor</a>, it enforces the rule that makes the flip-detector and stress-mode of the <a href="https://www.justinpurnell.com/QualityGate/project-health/test">test runner</a> meaningful: a suite that’s deterministic to begin with, so a flip is a <em>race</em>, not the clock.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check temporal-determinism
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/safety-security/stochastic-determinism</guid><title>&quot;StochasticDeterminismAuditor: The .random() That Fails One Build in Fifty&quot;</title><link>https://www.justinpurnell.com/QualityGate/safety-security/stochastic-determinism</link><description><![CDATA[Unseeded randomness in production and tests — the source of the flake you can never reproduce.]]></description><pubDate>Sat, 15 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[safety]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Unseeded randomness in production and tests — the source of the flake you can never reproduce.</strong></p><hr /><p>A test that uses <code>Int.random(in:)</code> or <code>.shuffled()</code> with the system RNG passes forty-nine times and fails the fiftieth, on a machine you don’t own, with a value you can’t recreate. The bug isn’t in the code under test — it’s in the fact that the test isn’t <em>deterministic</em>. Non-reproducible failures are the most expensive kind, because you can’t even confirm you’ve fixed them.</p><h2>What it catches</h2><pre><code class="language-swift">// ❌ implicit system RNG — non-deterministic
let pick = options.randomElement()
let n = Int.random(in: 0..<100)
let deck = cards.shuffled()

// ✅ seed it — reproducible every run
var rng = SeededGenerator(seed: 42)
let pick = options.randomElement(using: &rng)
let n = Int.random(in: 0..<100, using: &rng)
</code></pre><p>The rule flags the implicit-RNG forms and points you at the seeded overloads. The project’s own rule is blunt: <strong>stochastic tests always use a seeded RNG, never implicit <code>.random()</code>.</strong> A tool that flags unseeded randomness had better not contain any — and its tests don’t.</p><p>This pairs with its sibling, <a href="https://www.justinpurnell.com/QualityGate/safety-security/temporal-determinism">TemporalDeterminismAuditor</a>: between them they close the two biggest sources of non-reproducible test behavior — randomness and the wall clock. Determinism isn’t pedantry; it’s the precondition for a test suite you can actually trust to tell you the truth.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check stochastic-determinism
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/safety-security/keychain-secrets</guid><title>&quot;KeychainSecretsChecker: The Token You Left in UserDefaults&quot;</title><link>https://www.justinpurnell.com/QualityGate/safety-security/keychain-secrets</link><description><![CDATA[Credentials written to UserDefaults instead of the Keychain — a plaintext leak that’s one line of code and one App Store review away from a breach.]]></description><pubDate>Fri, 14 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[safety]]></category><category><![CDATA[compliance]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Credentials written to <code>UserDefaults</code> instead of the Keychain — a plaintext leak that’s one line of code and one App Store review away from a breach.</strong></p><hr /><p><code>UserDefaults</code> is a plaintext <code>.plist</code> inside the app container: unencrypted at rest, and swept into iTunes/iCloud device backups. It’s the right tool for “has the user seen the tutorial.” It’s the wrong tool for an auth token. For a health or clinical app, a token in <code>UserDefaults</code> is a concrete breach surface. It’s also an easy, invisible mistake — which is exactly what a static check is for.</p><h2>What it catches</h2><pre><code class="language-swift">// ❌ a secret written to UserDefaults
UserDefaults.standard.set(accessToken, forKey: "authToken")
defaults["refreshToken"] = token
UserDefaults.standard.setValue(apiKey, forKey: "clientSecret")

// ✅ store secrets in the Keychain
</code></pre><h2>How it stays precise — the AST earns its keep</h2><p>A naïve version of this check cries wolf constantly. The real one is a two-pass SwiftSyntax analysis, and its precision guards are the whole point:</p><ul><li><strong>Receiver typing.</strong> It first records which identifiers are actually bound to a <code>UserDefaults</code> instance, so a same-named <code>.set(...)</code> on some other object never fires.</li><li><strong>The Bool/Int guard.</strong> A stored <code>true</code> or <code>3</code> is not a credential — so <code>set(true, forKey: "hasSeenTokenTutorial")</code> stays quiet even though the key contains “token.”</li><li><strong>Word-aware matching.</strong> <code>tokenizer</code> is not <code>token</code>; camelCase and acronyms are tokenized, so <code>apiKey</code> matches as a compound while <code>tokenizerConfig</code> does not.</li><li><strong>Severity by confidence.</strong> A string-literal secret key gates at <code>.error</code>; a match seen only in the stored value’s identifier softens to <code>.warning</code>.</li></ul><p>Both a config <code>allowKeys</code> list and an inline <code>// keychain:exempt</code> (recorded, never silent) suppress a site. <strong>14 tests</strong>; it validated end-to-end flagging a real <code>authToken</code> write while leaving the adjacent boolean line alone.</p><h2>It’s also a compliance control</h2><p><code>keychain-secrets</code> is one of the technical-control checkers the <a href="https://www.justinpurnell.com/QualityGate/compliance/coverage-not-a-badge">compliance mapping</a> ties to HIPAA §164.312(a)(2)(iv) (encryption), SOC 2 CC6.1, and ISO 27001 A.8.24 — a good example of a single AST rule doing double duty: catching a bug <em>and</em> producing audit evidence.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check keychain-secrets
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/safety-security/safety</guid><title>&quot;SafetyAuditor: No Force Unwraps, and the OWASP Mobile Top 10&quot;</title><link>https://www.justinpurnell.com/QualityGate/safety-security/safety</link><description><![CDATA[The workhorse. It enforces the zero-tolerance safety rules — and a real slice of the OWASP Mobile Top 10.]]></description><pubDate>Thu, 13 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[safety]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>The workhorse. It enforces the zero-tolerance safety rules — and a real slice of the OWASP Mobile Top 10.</strong></p><hr /><p>If quality-gate-swift has a flagship checker, it’s this one. It enforces the rules that keep a shipping app from crashing on its users, and the security patterns that keep it from leaking them.</p><h2>The crash-safety rules</h2><pre><code class="language-swift">// ❌ force-unwrap — crashes on the one nil you didn't expect
let view = optionalView!
// ✅ guard let view = optionalView else { return }

// ❌ force-cast
let cell = item as! Cell
// ✅ guard let cell = item as? Cell else { return }

// ❌ force-try, and ❌ fatalError()/precondition() in production
let data = try! decode(payload)
</code></pre><p>The zero-tolerance rules — no <code>!</code>, no <code>as!</code>, no <code>try!</code>, no <code>fatalError()</code> in production — are the ones that show up in every “the app crashed” postmortem. Each has an escape hatch for the genuinely-safe case, and the escape hatch is a recorded <code>// SAFETY:</code> comment stating <em>why</em>: never silent.</p><h2>The security rules — OWASP Mobile Top 10</h2><p>The same AST walk powers a set of security checks mapped to the OWASP Mobile Top 10:</p><pre><code>security.hardcoded-secret     security.insecure-transport
security.insecure-keychain    security.tls-disabled
security.weak-crypto          security.command-injection
security.sql-injection        security.path-traversal
security.ssrf                 security.eval-js
</code></pre><p>Hardcoded credentials, HTTP where HTTPS is required, weak crypto (MD5/SHA-1/DES), disabled TLS validation, SQL built by string interpolation, path traversal, server-side request forgery from dynamic URLs. These aren’t hypothetical: <code>security.insecure-transport</code> and <code>security.weak-crypto</code> are exactly the rules the <a href="https://www.justinpurnell.com/QualityGate/compliance/coverage-not-a-badge">compliance mapping</a> later ties to HIPAA §164.312(e) and SOC 2 CC6.6.</p><p>A <code>// SECURITY:</code> comment exempts a validated case (a test fixture, a deliberately-allowlisted host) — recorded, with a reason, so an auditor can see every place a security rule was consciously relaxed.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check safety
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/correctness/legibility</guid><title>&quot;LegibilityAnalyzer: A Reading Order for a Codebase You&apos;ve Never Seen&quot;</title><link>https://www.justinpurnell.com/QualityGate/correctness/legibility</link><description><![CDATA[The most unusual checker in the suite: it never gates. It hands you a map.]]></description><pubDate>Wed, 12 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[correctness]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>The most unusual checker in the suite: it never gates. It hands you a map.</strong></p><hr /><p>Every other checker answers “is this line wrong?” <code>LegibilityAnalyzer</code> answers a different question: <em>“if I dropped you into this codebase cold, where would you start reading, and what would trip you up?”</em> It’s purely advisory — it never blocks a commit — and instead of a list of violations it emits a <strong>reading-order / module-map artifact.</strong></p><h2>What it surfaces</h2><ul><li><strong>Central-but-unoriented modules</strong> — a module that half the package depends on, but whose own purpose isn’t clearly stated. High blast radius, low legibility: exactly where a newcomer (human or AI) gets lost.</li><li><strong>Dependency cycles</strong> — modules that import each other, so there’s no clean order to read them in.</li><li><strong>Over-public surface</strong> — types and functions exposed as <code>public</code> that nothing outside their module actually uses. Every unnecessary <code>public</code> is a promise you didn’t mean to make and a reader you didn’t mean to confuse.</li></ul><h2>Why “flag to understand, not forbid” matters here</h2><p>This checker is the purest expression of the project’s advisory philosophy. None of these are <em>errors</em>. A central module might be central for good reason; some <code>public</code> surface is genuinely API. The analyzer’s job isn’t to forbid — it’s to make the <em>shape</em> of the codebase legible so you can decide what to orient, what to break, and what to narrow. It produces a module map you can hand to a new contributor (or paste into an AI session) so they read in the right order instead of wandering.</p><p>A gate that only ever says “no” makes code <em>correct</em>. A tool like this helps make it <em>understandable</em> — and over a long-lived codebase, understandability is what keeps the correctness from decaying.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check legibility
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/correctness/complexity</guid><title>&quot;ComplexityAnalyzer: Cognitive Load, and the Cost That Hides in the Call Graph&quot;</title><link>https://www.justinpurnell.com/QualityGate/correctness/complexity</link><description><![CDATA[Cognitive complexity per function — plus the complexity that isn’t in any single function, but in how they call each other.]]></description><pubDate>Tue, 11 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[correctness]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Cognitive complexity per function — plus the complexity that isn’t in any single function, but in how they call each other.</strong></p><hr /><p>Most complexity tools stop at one function: count the branches, the nesting, the operators, print a number. <code>ComplexityAnalyzer</code> does that too (<code>complexity.cognitive-threshold</code>), scoring cognitive load in a way that penalizes nesting more than flat sequences — because a function with three nested <code>if</code>s is harder to hold in your head than one with three sequential ones.</p><p>But the more interesting rules look <em>between</em> functions.</p><h2>Amplification: complexity you can’t see locally</h2><pre><code>complexity.call-graph-amplification     — a simple-looking function whose callees make it expensive
complexity.cross-module-amplification   — cost that compounds across module boundaries
</code></pre><p>A function can look trivial and still be a problem, because it calls three functions that each call three more. The <strong>call-graph amplification</strong> rule follows the calls and surfaces the <em>amplified</em> cost — the complexity a reader pays when they actually trace what the function does. The <strong>cross-module</strong> variant tracks that cost as it compounds across package boundaries, where it’s hardest to see and most expensive to untangle.</p><p>This is advisory by design — it never blocks. Complexity is a signal to understand, not a law to obey; sometimes the amplified path is genuinely necessary. The analyzer’s job is to make the cost <em>visible</em> so the decision is deliberate. (Where the IndexStore isn’t available, the cross-module pass reports <code>complexity.index-pass.skipped</code> and stays quiet rather than guessing.)</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check complexity
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/correctness/process-safety</guid><title>&quot;ProcessSafetyAuditor: The 64 KB Pipe Deadlock It Caught in Its Own Author&apos;s Code&quot;</title><link>https://www.justinpurnell.com/QualityGate/correctness/process-safety</link><description><![CDATA[Unsafe process spawning and command-injection patterns — including a classic deadlock that hangs forever, silently.]]></description><pubDate>Mon, 10 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[correctness]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Unsafe process spawning and command-injection patterns — including a classic deadlock that hangs forever, silently.</strong></p><hr /><p>Shelling out from Swift is deceptively easy to get wrong. <code>Process</code> + <code>Pipe</code> looks innocent, but there’s a trap that doesn’t show up until output crosses a threshold: if you spawn a process, call <code>waitUntilExit()</code>, and <em>then</em> read the pipe, you deadlock the moment the child writes more than the pipe buffer (~64 KB on macOS). The child blocks writing; the parent blocks waiting; nothing moves. Forever.</p><h2>What it catches</h2><pre><code class="language-swift">// ❌ process.wait-before-read — read the pipe AFTER waiting: deadlocks past ~64 KB
let pipe = Pipe()
process.standardOutput = pipe
try process.run()
process.waitUntilExit()                                  // ← child fills the pipe and blocks
let data = pipe.fileHandleForReading.readDataToEndOfFile()

// ✅ drain the pipe before (or concurrently with) waiting
let data = pipe.fileHandleForReading.readDataToEndOfFile()
process.waitUntilExit()
</code></pre><p><strong>Rule ID:</strong> <code>process.wait-before-read</code>. It also flags command-injection shapes — interpolated shell strings, unsanitized arguments passed to <code>/bin/sh -c</code>.</p><h2>Caught in its own author’s code</h2><p>The best endorsement I can give this checker is that it caught the exact bug in <em>my</em> code. While building the CI telemetry-push path — the code that publishes gate results to the corpus — the very first commit attempt had a <code>waitUntilExit()</code> before the pipe read. Under a small test fixture it passed; against real gate output it would have hung the moment the report crossed 64 KB. ProcessSafetyAuditor flagged it before it ever ran. That’s the whole argument for the tool in one incident: the bug that only appears under load, caught at commit time instead of at 2 a.m.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check process-safety
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/correctness/unreachable</guid><title>&quot;UnreachableCodeAuditor: Dead Code, and the 1,064 False Positives It Taught Me to Kill&quot;</title><link>https://www.justinpurnell.com/QualityGate/correctness/unreachable</link><description><![CDATA[Finding genuinely dead code — and the story of why precision is the whole product.]]></description><pubDate>Sun, 09 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[correctness]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Finding genuinely dead code — and the story of why precision is the whole product.</strong></p><hr /><h2>What it catches</h2><pre><code class="language-swift">// ❌ unreachable.after_terminator — code after a return/throw/fatalError
func f() -> Int {
    return 1
    print("never runs")   // ← dead
}

// ❌ unreachable.dead_branch — a branch that can never be taken
// ❌ unreachable.unused_private — a private symbol nothing references
// ❌ unreachable.cross_module.unreachable_from_entry — a public API no entry point reaches
</code></pre><p>The syntactic rules (<code>after_terminator</code>, <code>dead_branch</code>, <code>unused_private</code>) walk the AST. The cross-module rules go further: they cross-reference against the <strong>IndexStore</strong> to find symbols unreachable from any entry point across the whole package.</p><h2>The 1,064-finding lesson</h2><p>The first time the cross-module pass ran against a real app — Narbis, a multi-package biofeedback workspace — it emitted <strong>~1,064</strong> <code>unreachable.cross_module</code> findings. Almost all of them were in a <em>vendored SDK</em> the team didn’t own and couldn’t change.</p><p>A gate that produces 1,064 findings on its first run is a gate people delete. This was the moment the project’s central principle got real: <strong>precision isn’t a nicety, it’s the product.</strong> Tightening the checker (correct handling of vendored code, and — critically — <em>skipping the cross-module pass on a stale index</em> rather than emitting wrong-line findings) drove Narbis from <strong>1,064 findings to 0.</strong></p><p>That stale-index rule (<code>unreachable.cross_module.stale</code>) is worth dwelling on. A stale IndexStore records each symbol at its <em>old</em> line; once source above it shifts, every symbol below reports at the wrong line, and <code>// LIVE:</code> exemption matching misses, so live symbols look dead. Treating a stale index like a missing one — emit a <code>.note</code> pointing at a rebuild, don’t run against data you can’t trust — keeps the gate from failing on drift. The syntactic pass still runs.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check unreachable
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/correctness/memory-lifecycle</guid><title>&quot;MemoryLifecycleGuard: Tasks That Never Cancel, Delegates That Never Die&quot;</title><link>https://www.justinpurnell.com/QualityGate/correctness/memory-lifecycle</link><description><![CDATA[Retain cycles, un-cancelled tasks, and unbounded streams — the memory bugs that don’t crash, they just leak.]]></description><pubDate>Sat, 08 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[correctness]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Retain cycles, un-cancelled tasks, and unbounded streams — the memory bugs that don’t crash, they just leak.</strong></p><hr /><p>Memory bugs in Swift rarely announce themselves. A stored <code>Task</code> that’s never cancelled keeps running after its owner is gone. A <code>strong</code> delegate quietly forms a retain cycle. An <code>AsyncStream</code> with no back-pressure grows without bound. The app doesn’t crash — it just gets slower, warmer, and stranger over hours.</p><h2>What it catches</h2><pre><code class="language-swift">// ❌ lifecycle-task-no-cancel — a stored Task with no cancellation
final class Loader {
    private var task: Task<Void, Never>?
    func start() { task = Task { await load() } }   // never cancelled
}

// ❌ lifecycle-strong-delegate — a strong delegate reference (retain cycle)
var delegate: MyDelegate?          // should be `weak var`

// ❌ lifecycle-unbounded-stream — an AsyncStream with no termination path
// ❌ lifecycle-task-no-deinit — a Task-owning type with no deinit to cancel it
</code></pre><p>Representative rule IDs: <code>lifecycle-task-no-cancel</code>, <code>lifecycle-task-no-deinit</code>, <code>lifecycle-strong-delegate</code>, <code>lifecycle-unbounded-stream</code>, <code>lifecycle-stream-terminated-elsewhere</code>, <code>lifecycle-delegate-retained-elsewhere</code>.</p><h2>Cross-file lifecycle analysis</h2><p>The interesting part is what makes it precise. A <code>Task</code> stored in one file might legitimately be cancelled in another; a delegate declared <code>strong</code> here might be retained safely there. So the guard does <strong>cross-file</strong> analysis — the <code>-elsewhere</code> rules exist specifically to suppress a finding when the cancellation or the ownership actually lives in a different file. That’s the difference between a checker that nags and one you can leave on: it looks at the whole picture before it fires. Stale exemptions (<code>lifecycle-stale-exemption</code>) get flagged too, so suppressions don’t rot.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check memory-lifecycle
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/correctness/fp-safety</guid><title>&quot;FloatingPointSafetyAuditor: The == You Should Never Write&quot;</title><link>https://www.justinpurnell.com/QualityGate/correctness/fp-safety</link><description><![CDATA[Two of the most common numerical bugs in Swift: exact floating-point equality, and division with no zero-guard.]]></description><pubDate>Fri, 07 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[correctness]]></category><category><![CDATA[swift]]></category><content:encoded><![CDATA[<p><strong>Two of the most common numerical bugs in Swift: exact floating-point equality, and division with no zero-guard.</strong></p><hr /><p>Floating-point is where confident code quietly goes wrong. <code>0.1 + 0.2 == 0.3</code> is <code>false</code>. A denominator that’s usually positive is occasionally zero. Neither is a compile error; both are real bugs, and both showed up in the <a href="https://www.justinpurnell.com/QualityGate/gate/a-gate-not-a-linter">fifty-five-error incident</a> that started this whole project.</p><h2>What it catches</h2><pre><code class="language-swift">// ❌ fp-equality — exact equality on floating-point
XCTAssertEqual(score, 0.85)
if price == 19.99 { … }

// ✅ compare with a tolerance
XCTAssertEqual(score, 0.85, accuracy: 1e-6)
if abs(price - 19.99) < .ulpOfOne { … }

// ❌ fp-division-unguarded — division with no zero-check
let mean = total / count            // count can be 0

// ✅ guard the denominator
guard count > 0 else { return 0 }
let mean = total / count
</code></pre><p><strong>Two rule IDs:</strong> <code>fp-equality</code>, <code>fp-division-unguarded</code>. Both walk the AST, so they distinguish a genuine floating-point <code>==</code> from an integer comparison or a <code>==</code> inside a string, and they see the actual division operator in context rather than pattern-matching a <code>/</code>.</p><p>The tests in quality-gate-swift itself follow the rule they enforce: never <code>==</code> for floating-point, always <code>abs(a - b) < 1e-6</code>; <code>T.ulpOfOne</code> for near-zero checks in production. A checker that flags exact equality had better not ship any.</p><h2>Try it</h2><pre><code class="language-bash">quality-gate --check fp-safety
</code></pre><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/mirror/the-mirror</guid><title>&quot;The Mirror: Teaching an Organization to Learn From Its Own Decisions&quot;</title><link>https://www.justinpurnell.com/QualityGate/mirror/the-mirror</link><description><![CDATA[quality-gate is the gate. The Institutional Judgment System is the mirror — it captures the reasoning behind every override and feeds that memory back into the next run.]]></description><pubDate>Thu, 06 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[mirror]]></category><content:encoded><![CDATA[<p><strong>quality-gate is the gate. The Institutional Judgment System is the mirror — it captures the reasoning behind every override and feeds that memory back into the next run.</strong></p><hr /><p>Every team makes judgment calls. A quality gate flags a warning and someone ships anyway. A safety check fails and a senior engineer overrides it with “we’ll fix it next sprint.” These moments — the overrides, the exemptions, the calculated risks — are where institutional knowledge lives and dies.</p><p>Most organizations treat them as noise. The override happens, the build ships, and the reasoning evaporates. Three months later a different engineer faces the same trade-off with zero context about what happened last time.</p><p>The <strong>Institutional Judgment System</strong> (IJS) — the <code>org-judgement-system</code> and its <code>org-judgement-corpus</code> — exists to fix that. It’s a four-layer feedback loop that captures override decisions, analyzes them statistically, detects recurring patterns, and feeds that institutional memory back into every subsequent gate run. It’s not a linter and it’s not a policy engine. It’s an organizational immune system.</p><h2>The core insight is Ray Dalio’s</h2><p>Bridgewater’s <em>Principles</em> describes a five-step loop for organizational learning: <strong>Goals → Problems → Diagnosis → Design → Doing.</strong> Most engineering tools stop at <em>Problems</em> — they tell you something is wrong. The IJS maps every override and failure through the full five steps, tracking not just <em>what</em> went wrong but <em>which thinking capability</em> broke down. A team that keeps failing at <em>Diagnosis</em> (they see the problem but misidentify the root cause) needs a different intervention than one failing at <em>Design</em> (they diagnose right but choose poorly).</p><p>That’s the Bridgewater move applied to software: turn vague “we need to do better” into specific “our diagnosis capability needs calibration.” Principles, made into algorithms, made into a machine.</p><h2>Four layers, one loop</h2><p><strong>Layer 1 — Sensor.</strong> When a gate runs and someone overrides a failure, the system records a structured <code>JudgmentCalibration</code>: the override reasoning, a risk tier that determines who has authority to make the call, a root-cause analysis that separates the proximate cause from the decision process that failed, and <strong>mandatory red-team dissent</strong> — even when you’re right to override, you must articulate the counterargument. Root-cause adjectives describe <em>processes</em>, never people: “rushed,” “underspecified,” never “incompetent.” Learning without blame. A <code>DecisionResponsibilityMatrix</code> prevents <em>decision compression</em> — one person quietly occupying every role (architect, reviewer, override authority, sign-off) on a quick fix.</p><p><strong>Layer 2 — Aggregator.</strong> A <code>TelemetryWriter</code> actor collects the raw decisions into a persistent, deterministic corpus. The design constraint is that <strong>every write is lossless</strong>: ISO-8601 dates, sorted JSON keys, human-readable paths. The corpus is built to be <em>inspected by humans</em>, not just machines. This is radical transparency as a file format.</p><p><strong>Layer 3 — Refiner.</strong> Here the system starts thinking. It computes an <code>InstitutionalPulse</code> — trend analyses with confidence intervals, statistical anomalies at the 90th/95th/99th percentiles, violation clusters (the same rule overridden across projects for weeks), and human-readable calibration summaries. Every result carries a <code>StatisticalValidity</code> grade from the Central Limit Theorem: fewer than 3 samples is <em>insufficient</em>, 3–29 <em>preliminary</em>, 30+ <em>valid</em>. Early on, the system flagged “anomalies” from two data points — meaningless noise that eroded trust. Validity grading, and discounting scores by it, is what made the output trustworthy. (The principle is borrowed from how NASA’s Artemis program treats sparse sensor data.)</p><p><strong>Layer 4 — PolicyDiscovery.</strong> The loop closes. On the next gate run, the <code>PolicyDiscoveryAuditor</code> compares current failures against the latest Pulse and asks: does this match a known violation cluster (institutional drift)? Does this checker’s failure rate deviate from baseline (a systemic issue, not a one-off)? Was a policy change proposed for this pattern and never implemented (institutional debt)? Each match is a <code>ConsistencyFinding</code>, and the <code>ConsistencyScorer</code> rolls them into a single score — 1.0 is “fully consistent with institutional history,” 0.4 is “this looks like drift; the Pulse shows patterns you should know about.” The discounting is validity-aware: a finding from 3 data points carries a quarter the weight of one from 30+.</p><p>So every gate run now ends with an institutional consistency score. The gate told you whether the code passes. The mirror tells you whether the <em>decision</em> is consistent with everything the organization has learned.</p><h2>Believability, not approval</h2><p>We first imagined an approval workflow for exemptions. In practice, what matters is the <em>justification</em>, not the sign-off. “This cluster match is expected — we’re mid-migration” is worth more than a rubber stamp, because the justification lets a future session judge whether the exemption still holds. That’s believability-weighting in the Dalio sense: input earns weight through recorded reasoning and track record, not through seniority.</p><h2>The point</h2><p>The IJS doesn’t replace human judgment — it makes it <em>visible, trackable, and improvable.</em> Every override is a learning event. Every Pulse is a progress report. Every consistency score is a mirror.</p><p>The question was never “how do we prevent bad decisions?” It was “how do we help an organization learn from the decisions it’s already making?”</p><p>The posts that follow go layer by layer — the corpus, the pulse, policy discovery, calibration and the judgment workbench, the trust service — and one essay on the deeper lineage: <strong>Principles as Software.</strong></p><hr /><p><em>Built with Swift 6, strict concurrency, SwiftSyntax, BusinessMath’s statistics, and a deep suspicion of silent overrides.</em></p><p><strong>Source:</strong> <a href="https://github.com/jpurnell/org-judgement-system">github.com/jpurnell/org-judgement-system</a></p>]]></content:encoded></item><item><guid isPermaLink="true">https://www.justinpurnell.com/QualityGate/gate/a-gate-not-a-linter</guid><title>&quot;A Gate, Not a Linter&quot;</title><link>https://www.justinpurnell.com/QualityGate/gate/a-gate-not-a-linter</link><description><![CDATA[The philosophy behind quality-gate-swift: why mechanical enforcement beats good intentions, and what it takes to earn the right to block a commit.]]></description><pubDate>Wed, 05 Aug 2026 09:00:00 +0000</pubDate><dc:creator><![CDATA[Justin Purnell]]></dc:creator><category><![CDATA[quality-gate]]></category><category><![CDATA[gate]]></category><content:encoded><![CDATA[<p><strong>The philosophy behind quality-gate-swift: why mechanical enforcement beats good intentions, and what it takes to earn the right to block a commit.</strong></p><hr /><h2>The fifty-five error incident</h2><p>We had thirteen documents. Sixty-one kilobytes of coding rules. A TDD contract with a mandatory VERIFY step. A session workflow. Design-proposal templates. Release checklists. Floating-point formatting guides.</p><p>None of them prevented us from shipping fifty-five errors and fifty-three warnings in a single batch of commits.</p><p>The failure happened during a sprint to build new modules for the Institutional Judgment System — the very system designed to help organizations learn from their mistakes. The workflow was followed: DESIGN, RED, GREEN, REFACTOR, DOCUMENT. Tests first. But the workflow has <em>six</em> steps, and the sixth is VERIFY — run the quality gate before committing. It got skipped. Every time. Across dozens of commits.</p><p>Floating-point exact equality in tests. Unreachable dead code left from refactoring. Public APIs with no documentation — in a project whose own doc-coverage checker flags exactly that. Silent error swallowing. Fifty-five errors, in the tool built to prevent these exact problems.</p><p>The root cause was not a lack of documentation. The documents were fine. The root cause was that <strong>documentation describes the ideal process, and nothing forced adherence to it.</strong></p><p>That crystallized the principle underneath the whole project:</p><blockquote><p><strong>If a rule matters, it needs a hook, not a document.</strong></p></blockquote><p>A linter is a document with syntax highlighting. It tells you what’s wrong and lets you ship anyway. A <strong>gate</strong> is a hook. It doesn’t happen when you remember; it happens every time, and when it fails, the commit does not happen.</p><h2>To block, you must be right</h2><p>The moment you decide to <em>block</em> rather than <em>warn</em>, everything changes. A warning can be 60% accurate and still be useful. A gate that’s 60% accurate is a gate people rip out within a week — and rightly so. An ignored checker is worse than no checker, because it trains people to skip gate output entirely.</p><p>So the entire design is organized around one demand: <strong>nearly every finding must be real.</strong> That single constraint drives every technical choice in the tool.</p><h3>It reads the tree, not the text</h3><p>Most quality problems in Swift aren’t typos — they’re structural. A force-unwrap that crashes on the one <code>nil</code> you didn’t expect. An <code>async</code> stream whose cancellation path was never specified. A pointer that escapes the block that owned it. These are invisible to a regex, which can’t tell <code>value!</code> from <code>value != nil</code> from a <code>!</code> inside a string literal.</p><p>Every rule in quality-gate-swift walks the Swift <strong>abstract syntax tree</strong> via SwiftSyntax. It knows scope, type context, and control flow. That’s what makes it precise enough to block. Zero regex — 33 checkers, all AST-based.</p><h3>Precision is the product</h3><p>Precision isn’t a nicety; it <em>is</em> the deliverable. Two numbers make the point. The first run of the dead-code checker against a real app (Narbis) emitted <strong>~1,064 findings</strong> — almost all from a vendored SDK the team couldn’t change. A gate that cries wolf 1,064 times is one people learn to ignore. Tightening the checker drove that to <strong>0</strong>. Later, a noisy duplication pass produced <strong>14,000+</strong> findings; the same discipline collapsed it to <strong>11</strong> genuine ones.</p><p>Every hour spent killing a false-positive class buys the right to keep blocking.</p><h3>Recorded, never silent</h3><p>Zero-tolerance doesn’t mean zero exemptions. It means <strong>no silent ones.</strong> Every exemption is a single inline comment that states <em>why</em> — <code>// SAFETY:</code>, <code>// Justification:</code>, <code>// keychain:exempt</code> — recorded, dated, and searchable. You can see every place the rules were consciously relaxed, and so can the next person. There is no <code>--no-verify</code> culture, no backlog of ignored warnings.</p><h2>The architecture that makes it cheap</h2><p>The reason this scales is a single protocol:</p><pre><code class="language-swift">public protocol QualityChecker: Sendable {
    var id: String { get }
    var name: String { get }
    func check(configuration: Configuration) async throws -> CheckResult
}
</code></pre><p>That’s the whole contract. Implement it, return a <code>CheckResult</code> with diagnostics, and register the module in one line. The reporters (terminal, JSON, SARIF for GitHub Code Scanning, Xcode build phase), the config loader, the parallel runner, the exit-code wiring — all of it works automatically.</p><p>The cost of adding a new check is proportional to the complexity of the <em>check</em>, not the complexity of <em>integrating</em> it. That matters more than it sounds. When integration is expensive, you don’t add checks — you write the rule in a style guide, tell yourself you’ll remember, and the bugs ship anyway. When integration is one new file and one line, you add the check. And then the bugs don’t ship.</p><h2>Flag to understand, not forbid</h2><p>Not every checker blocks. Some are advisory by design — complexity, legibility, smells, duplication — surfacing unusual-but-legitimate code with a recorded acknowledgment rather than a hard stop. The rule for advisory tools is strict: <strong>earn your keep or get removed.</strong> If a checker’s findings are consistently ignored, the correct response is to disable it, not to force compliance. An advisory checker exists to help you <em>understand</em> the shape of the codebase, not to boss it around.</p><p>The gate blocks on correctness and safety. It informs on everything else. Keeping that line clean is what keeps the gate trusted.</p><h2>It’s held to its own standard</h2><p>quality-gate-swift runs its own 33 checkers on every push. Its ~1,700 tests are the tool held to the standard it enforces. When it finds a false positive, it finds it against itself first — like the recursion checker flagging its own SwiftSyntax visitor, caught and fixed before it ever shipped to another project. Dogfooding isn’t a virtue signal here; it’s the primary test harness.</p><h2>The gate is half the story</h2><p>A gate tells you <em>no</em>. It’s mechanical, unambiguous, and it never forgets to run. But it has no memory of <em>why you said yes</em> the last time you overrode it. It can’t tell you that this exact exemption has been taken four times across three projects, or that the reasoning that justified it last quarter no longer holds.</p><p>That’s the other half — the <strong>Mirror</strong> — and it’s where this project stops being a linter and starts being an organizational nervous system. But the Mirror only works because the Gate is honest: every block, every exemption, every reason, recorded and never silent. Enforcement first. Then memory.</p><hr /><p><strong>Source:</strong> <a href="https://github.com/jpurnell/quality-gate-swift">github.com/jpurnell/quality-gate-swift</a></p>]]></content:encoded></item></channel></rss>