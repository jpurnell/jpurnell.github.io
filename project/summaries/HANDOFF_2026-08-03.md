# HANDOFF — 2026-08-03 — "judgement-marketing" session

Start here to pick up. This session spanned **two projects** and one artifact.
Everything is committed and pushed; nothing is mid-flight.

## The two threads

### 1. quality-gate-swift — SOC2/ISO/HIPAA compliance initiative (COMPLETE, pushed)
Repo: `Tools/quality-gate-swift`, `origin/main` (8 commits `7898ccf`→`ad84a35`).
- `keychain-secrets` + `privacy-manifest` checkers.
- `control-mapping` checker (integrity of the rule→control mapping) + bundled
  registry/catalogs (HIPAA §164.312 verbatim; SOC2/ISO paraphrase+citation).
- `quality-gate compliance` report (coverage matrix; never asserts "compliant").
- `quality-gate standards-watch` — eCFR drift detection (HIPAA armed) +
  Federal Register NPRM early warning.
- Per-phase summaries in that repo's `project/summaries/`;
  proposal `RegulatoryControlMapping.md` (marked COMPLETE).

### 2. justinpurnell.com — *The Gate and the Mirror* web experience (COMPLETE, published)
Repo: `justinpurnell.com`, `origin/main` → `jpurnell.github.io` (10 commits
`5fe54f0`→`6408385`). See `2026-08-03_QualityGate_WebExperience.md` for detail.
Live: <https://www.justinpurnell.com/quality-gate>. 49 posts + `.epub`.

### 3. Marketing artifact
`Tools/quality-gate-swift/docs/quality-gate-swift.html` — **untracked on
purpose** (it's the source of the published Claude artifact:
`https://claude.ai/code/artifact/0348e6f2-a529-4ec0-a645-61c98f783f5a`). Leave it
untracked, or commit if you want it in the repo — your call.

## State: clean
- quality-gate-swift: pushed (ahead 0); only the untracked marketing HTML.
- justinpurnell.com: pushed (ahead 0).
- This dev-guidelines repo (`project-state/justinpurnell-com`): plan +
  summaries committed locally. **Never push development-guidelines.**

## Open / next items (nothing blocking)
1. **standards-watch deployment (the one operational to-do).** The weekly
   launchd job for `standards-watch` is NOT yet deployed on roseclub — it needs
   your SSH + sudo. Runbook: `Tools/quality-gate-swift/scripts/standards-watch/README.md`.
2. **Epub re-copy** if the site content changes: Ignite doesn't copy `.epub`
   from `Content/`; re-run the pandoc build and `cp` it into `docs/QualityGate/`
   after `swift run` (see the web-experience summary).
3. **Optional polish** on the web section: a per-project compliance dashboard
   tile (deferred, with rationale, in `RegulatoryControlMapping.md`); a fuller
   epub incorporating more of the book draft's chapters; a richer hero graphic.

## Gotchas
- Don't push the `development-guidelines` repos (plans stay local).
- Site URLs: landing `/quality-gate`, posts `/QualityGate/...` (capitalized).
- Shared checkouts: commit with explicit pathspecs (other sessions may touch
  `.claude/settings.local.json`, `CHANGELOG.md`, `docs/`).
